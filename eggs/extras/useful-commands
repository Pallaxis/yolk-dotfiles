makepkg -si # -s get dependencies -i install

sudo netstat -tunlp # list all listening ports

stow --adopt . # overwrites git dir with current dotfiles
git restore . # restores git dir to main branch

pacman -Fl # lists files owned by package
pacman -F {query here} # searches all packages for files, useful for finding package that provides command
pacman -Qo # shows what package provides this command
pacman -Qet # packages explicitly installed and not required as deps

vi sort and replace:
Sort the file using :sort
Execute command :g/^\(.*\)$\n\1$/p
